# Hook Library

## Methods

### > Register( name: **string**, id: **string**, callback: **function(...)** )

The **callback** function may have parameters. It depends on the hook

### > Unregister( name: **string**, id: **string** )
