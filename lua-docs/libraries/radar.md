# Radar Library

### > GetRange( )
returns **int**

### > GetRadius( )
returns **number**

### > WorldToRadar( localPos: **Vector3**, enemyPos: **Vector3** )
returns **Vector3**

This returns the XY screen coordinates on the Radar window that enemyPos would be